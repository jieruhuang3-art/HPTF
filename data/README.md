# Data Directory

This repository does not include raw datasets, processed TSV files, split files, model weights, or experiment caches.

Place downloaded public datasets under `data/raw/`, generated TSV files under `data/processed/`, and split metadata under `data/splits/`.
